# Tarefas 01, 02 e 03 v2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Reimplementar as v2 com discovery UDP, eleicao deterministica, handshake TCP e loop de heartbeat da Sprint 1.

**Architecture:** O Master executa dois canais em paralelo: um listener UDP para discovery e um servidor TCP para handshake e heartbeat. O Worker envia discovery UDP, elege o Master, confirma por TCP e depois entra no loop de heartbeat reaproveitando o protocolo da Sprint 1.

**Tech Stack:** Python 3, socket, json, threading, time, re

---

### Task 1: Implementar Discovery UDP

**Files:**
- Modify: `master_a_v2_0.py`
- Modify: `worker_a1_v2_0.py`

- [x] Adicionar listener UDP no Master.
- [x] Adicionar envio UDP de discovery no Worker.
- [x] Adicionar resposta unicast do Master com `SERVER_UUID`, `TCP_HOST` e `TCP_PORT`.
- [x] Adicionar timeout de coleta de 3 segundos no Worker.

### Task 2: Implementar Eleicao Deterministica

**Files:**
- Modify: `worker_a1_v2_0.py`

- [x] Armazenar Masters descobertos temporariamente.
- [x] Implementar regra deterministica de escolha.
- [x] Seguir o exemplo do enunciado `MASTER_1 < MASTER_2 < MASTER_10` com comparacao por sufixo numerico.

### Task 3: Implementar Handshake TCP e Heartbeat

**Files:**
- Modify: `master_a_v2_0.py`
- Modify: `worker_a1_v2_0.py`

- [x] Adicionar payload TCP `ELECTION_CONFIRM`.
- [x] Adicionar `ACK` do Master para confirmacao de eleicao.
- [x] Iniciar loop de heartbeat da Sprint 1 apos o `ACK`.
- [x] Validar o heartbeat com `RESPONSE=ALIVE`.

### Task 4: Validar Reimplementacao

**Files:**
- Modify: `master_a_v2_0.py`
- Modify: `worker_a1_v2_0.py`

- [x] Validar sintaxe com `python -m py_compile`.
- [x] Validar smoke test local com discovery UDP, `ACK` de eleicao e 2 heartbeats.
- [x] Validar a regra de eleicao com o exemplo `MASTER_1`, `MASTER_2`, `MASTER_10`.